# Attachment summary

Source image: `210748804.png` (280×280, 8-bit RGB PNG, no text metadata).

A single centered emblem on a flat dark navy field (about `#031D40`, RGB 3, 29, 64). The mark is white, left-right symmetric, and has no readable text.

The glyph is a set of thick strokes (about 16–22px) that join into a solid rounded cap at the top. Under the cap they separate into five bands:

- an outer pair that bows outward into a wide arch, widest near the lower third of the mark, with rounded ends
- an inner pair of shorter strokes closer to the center
- a straight central bar that continues below the arches and ends in a short rounded stem

The gaps between strokes open into the background; there are no enclosed holes. It reads as a symmetric branching mark: nested arches over a central stem, similar to a simplified tree, signal, or broadcast icon.
